function multiplicacao(numero1: number, numero2: number): number {
    return numero1 * numero2
}

console.log(multiplicacao(6,6));

function saudacao(nome: string): string {
    return `Olá, ${nome}`;
};

console.log(saudacao('Micael'));